{
  "name": "typescript-type-utils",
  "description": "utility types",
  "types": "./lib/index.d.ts",
  "version": "0.1.0",
  "scripts": {
    "lint": "tslint -p tsconfig.json",
    "pretest": "yarn lint",
    "build": "tsc",
    "test": "yarn build"
  },
  "devDependencies": {
    "tslint": "^5.20.1",
    "typescript": "~3.7.4"
  },
  "files": [
    "lib"
  ],
  "license": "MIT"
}
